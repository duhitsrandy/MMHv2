# Run unit tests

1. Use the Shell tool to run: `npm run test`
2. Run in the foreground and wait for completion.
3. Report pass/fail counts and any failing test names.

If tests fail, offer to fix only with user confirmation.
